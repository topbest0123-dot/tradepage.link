/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // add other valid options here if you use them, e.g.:
  // images: { domains: ['images.unsplash.com'] },
};

module.exports = nextConfig;
